---
created_at: 2026-08-01T21:37:58Z
updated_at: 2026-08-01T22:34:41Z
completed_at: 2026-08-01T22:34:41Z
---

## 0. Approval and baselines

- [x] 0.1 Approve this proposal, design, and all three capability deltas before
  implementation.
- [x] 0.2 Preserve the existing uncommitted `Ctrl+C` footer changes and freeze
  current `/context`, configuration precedence, catalog, replay, and provider
  wire fixtures.

## 1. Honest stable context categories

- [x] 1.1 Update `DESIGN.md` first with baseline context rows, pre-plan unknown
  grammar, aggregation, zero-category, narrow-width, and no-color behavior.
- [x] 1.2 Aggregate instruction kinds for display only, always render system
  instructions and tool schemas, and retain canonical unaggregated telemetry.
- [x] 1.3 Add before-plan, absent-total, zero-total, estimated, exact, narrow,
  and no-color context tests with no provider spend.

## 2. Reasoning capability and request policy

- [x] 2.1 Add typed layered reasoning defaults and source-explainable control
  metadata without treating a reasoning boolean as controllability.
- [x] 2.2 Add exact OpenAI-effort, OpenRouter-reasoning, and Z.AI-thinking wire
  adapters plus pre-network capability validation and redaction-safe policy
  status.
- [x] 2.3 Add a session controller and immutable per-turn model interceptor so
  retries and tool continuations use one setting; persist, resume, revalidate,
  and inherit it into new children.
- [x] 2.4 Add deterministic request-body tests for supported, fixed, mandatory,
  unknown, off, default, effort, invalid, switched-model, resume, and child
  cases without enabling permissive downgrade.

## 3. Terminal controls and documentation

- [x] 3.1 Add `/think` and `/effort` registry entries, direct parsing, bounded
  selectors, local unavailable reasons, idle-only dispatch, and help text.
- [x] 3.2 Show effective reasoning state, effort, and provenance in `/status`
  and `/context`, plus a width-gated non-default footer hint.
- [x] 3.3 Update configuration/setup documentation and provider capability
  provenance guidance, including latency/token implications and unknown-model
  behavior.

## 4. Validation

- [x] 4.1 Run formatting, warning-denied Clippy, workspace/all-feature tests,
  MSRV, strict spec validation, diff hygiene, and real terminal QA at narrow,
  normal, and wide sizes.
- [x] 4.2 Reinstall the verified local `smith` binary; keep any Z.AI or
  OpenRouter live test explicit, credential-redacted, and spend-capped.
